{
  "$id": "lobby/joined_message.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Lobby - Joined message",
  "description": "",
  "type": "object",
  "required": [
    "user_id",
    "user_secret"
  ],
  "properties": {
    "user_id": {
      "description": "The ID of the user joining, needed for the host to know which user this is about but should be ignorable by the user who joined",
      "type": "string"
    },
    "user_secret": {
      "description": "A secret sent to the user and the host to allow the user to prove to the host their game client is linked to their server client",
      "type": "string"
    }
  }
}