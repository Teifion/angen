{
  "$id": "lobby/joined_message.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Lobby - Joined message",
  "description": "",
  "type": "object",
  "required": [
    "lobby",
    "shared_secret"
  ],
  "properties": {
    "lobby": {
      "$ref": "types/lobby/lobby.json"
    },
    "shared_secret": {
      "description": "A secret sent to the user and the host to allow the user to prove to the host their game client is linked to their server client",
      "type": "string"
    }
  }
}