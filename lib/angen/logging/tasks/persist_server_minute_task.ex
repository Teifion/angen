defmodule Angen.Logging.Tasks.PersistServerMinuteTask do
  @moduledoc """
  A task used to persist a `Angen.Logging.ServerMinuteLog`; [(can be extended)](config.html#logging_post_server_minute_collect).
  """

  def perform() do
    %{

    }
  end
end
